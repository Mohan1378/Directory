cloudwatch
how monitoring in aws cloud
Nagios,Zabbix,icinga - monitoring tools
devops focussecd on micro services like docker

real time monitoring toools 
datadog
sensu
new relic
app dynamic
tick|TIG - open source
promotheous

ELK and Splunk - Log monitoring
teams, telegram, slag , sms alerts in aws -coomunication tools
Simple Notification Service(SNS) service - used to through mail during monitoring
states -> alarm ,ok,insufficient (Always activate Ok state to notify back to normal )
ulimit in linux
Autoscaling

TIG | TICK
inflects data
Telegrad Influxdb(Time series database) 
chronograph (daskboard)
kapacitor(Alerting, we need to configure) 
grafana - display fancy UI and alert you
TIG- best to use
dmesg,sys log - to recover files from var FS
Add datasorce - influx db
 - ADD ENDPOINT AND SETTINGS 
- we can find telegraph plugins in google-git (input and output)


VPC virtual private connection
- isolated portion of aws
-netflix mostly uses aws
ELB route 53 or API

IP subnet mask gateway
ipv4 ipv6
class A-E subnet
1st ip for n/w and last for broadcast
1 for gateway
for subnet mask 10.8.68.56/27 28 machine can create - place one and kept 0 for rem
last multiply with 2^. subtract from 255 exclude 3 ip's you will get no.of IP can create in subnet 
lan,wan,man
10.0.0.0/8
172.31.0.0/16
192.168.0.0./24
internet gateway NAT gateway
CIDR block - inter domain routing
Route table -> iptables (port-ip restriction)
egress gateway is completely public
DHCP -Railwire
Elstic ip - public 
endpoint services - ELB load balancing
NAT
VPC peering conn
n/w ALC
site-to-site
tunnel vpn conn
we cannot create multiple vpc under 1 subnet



SCM
Continuous integration
poll scm
CI - workflow
CI- benefits 
CI Tools
code repo->svn,git
continuous build system ->Jenkins, bamboo, cruise control
Test framework-> Junit,cucumber
Artifactory->nexux,Archieve, artifactory


Jenkins
-java based continuous build system
Runs in servlet container
hpi- plugin format
8080- port no for jenkins
dedicated and on-demand 
docker container
~ -home dir

passwordless authentiation
ssh -copy -id
user@IP/domain
jenkins->configure global security
cross site request forgery

ports:
ssh -22
ping -23
http - 80
https -443
tomcat 8080
smtp -25,465
pop-110,993
imap -143,995
dns -53,
mysql -3306
postgress - 5432
grafana -3000
Influx -8083,8086

Global tool configuration

min hour dayofmont month dayof week - Build triggered
fingers filename


.filename ->hidden file
ls -a
ls -ltra

touch,mkdir,pwd, 
rm,cat,ls,ls -lf, grep, cs,cat,cp,mv ../
mkdie --help
cat config
cd objects
git,git hub,bitbucket, 
ssh-keygen , add key to git

ssh-keygen -t rsa
 cat ~/.ssh/id_rsa.pub
visualpath
ssh -p 29418 Mohan1378@github.com
stages in git -> working dir,indexing/staging area,local repo - global repo
git status
git add filename ->move to staging area
git add . ->all files
git log ->to see hash code
git commit -m "test commit"
git show hashcode

vim filename
to insert i
to escape esc key
save and exit :wq
exit without save :q
dd -delete
u -undo
ctrl+R ->reverse-i-search
/ - used to search
git config --list
git config --global
git config user.name "Mohan1378"
git config user.email "mohanbtechit137@gmail.com"

ls -a ->to see git config
cat .gitconfig
git push origin master
git pull origin master
git branch
git branch -a

